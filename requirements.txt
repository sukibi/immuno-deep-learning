# Main environment — everything except 1_features/cache_features_3d.py (CT-FM).
# The torch build is the tested one (CUDA 12.1); the extra index below fetches it.
# Regenerate an exact lock with:  pip freeze > requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu121

# deep learning
torch==2.5.1
torchvision==0.20.1
timm>=1.0.0

# CT I/O, segmentation, preprocessing
SimpleITK>=2.3.0
lungmask>=0.2.20
scipy>=1.11

# classical ML + evaluation
scikit-learn>=1.3
joblib>=1.3

# data handling
numpy>=1.24,<2.0
pandas>=2.0
openpyxl>=3.1          # read the .xlsx clinical/metadata workbooks

# figures
matplotlib>=3.7
