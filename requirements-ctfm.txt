# Separate environment for 1_features/cache_features_3d.py (CT-FM encoder).
# lighter-zoo pulls in a matching MONAI/torch; keep this venv isolated from the main one.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1
torchvision==0.20.1
lighter-zoo
monai==1.5.2
numpy>=1.24,<2.0
pandas>=2.0
